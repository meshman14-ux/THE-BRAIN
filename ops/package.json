{
  "name": "opsdeck",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "OPSDECK — event staffing / suitability / stock / readiness demo app.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.9"
  }
}
