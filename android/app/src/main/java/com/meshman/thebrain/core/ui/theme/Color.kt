package com.meshman.thebrain.core.ui.theme

import androidx.compose.ui.graphics.Color

// THE BRAIN's brand palette — the blue→violet identity carried over from the
// web hub. Used as the fallback color scheme when Material You dynamic color is
// unavailable or switched off, so the app always looks like *itself*.

val BrandBlue = Color(0xFF2F6FED)
val BrandViolet = Color(0xFF7B4BD6)
val BrandBlueLight = Color(0xFF6EA8FE)

// Dark scheme surfaces
val DarkBackground = Color(0xFF0F1115)
val DarkSurface = Color(0xFF171A21)
val DarkSurfaceVariant = Color(0xFF262B36)
val DarkOnSurface = Color(0xFFE8EAED)
val DarkOnSurfaceMuted = Color(0xFF9AA0AC)

// Light scheme surfaces
val LightBackground = Color(0xFFF5F6F8)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFE3E6EB)
val LightOnSurface = Color(0xFF1A1C20)
val LightOnSurfaceMuted = Color(0xFF5C6270)

// Accent used for streaks / positive states
val Success = Color(0xFF4EC98F)
val Danger = Color(0xFFD64541)
