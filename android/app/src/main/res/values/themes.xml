<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose draws all UI and handles light/dark + Material You itself
      (see core/ui/theme/Theme.kt). This XML theme only needs to give the
      window a sane background and hide the action bar before Compose starts.
      Using an android:* parent avoids pulling in the Material Components XML lib.
    -->
    <style name="Theme.TheBrain" parent="android:Theme.Material.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
